single table design and overloaded GSI DynamoDB POC

usage:
 - build dist and zip `npm run build`
 - deploy resources
   - ```shell
        cd terraform
        terraform init 
        terraform plan
        terraform apply
     ```
 - destroy resources:
    ```shell
      cd terraform
      terraform destroy
    ```
